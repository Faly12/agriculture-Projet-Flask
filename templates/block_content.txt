<div id="productList" class="row row-cols-1 row-cols-md-4 g-4">
    {% for produit in produits %}
    <div class="col product-card">
      <div class="card h-100">
        <img src="{{ url_for('static', filename='uploads/' ~ produit.image) }}" class="card-img-top" alt="{{ produit.nom }}">
        <div class="card-body">
          <h5 class="card-title">{{ produit.nom }}</h5>
          <p class="card-text">{{ produit.description }}</p>
          <p class="card-text text-success fw-bold">{{ produit.prix }} Ar</p>
        </div>
      </div>
    </div>
    {% endfor %}
  </div>